package com.syntax.class19;

import com.syntax.class20.Laptop;

public class AppleLaptop extends Laptop {
    void printMake(){
        //can`t access because we are in different package
        //if its default but

        System.out.println();
    }
}
